name: qiita_client_flutter
description: Qiita Client Flutter App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks: ^0.17.0
  hooks_riverpod: ^0.14.0
  state_notifier: ^0.7.0

  dio: ^4.0.0
  retrofit: ^2.0.0

  json_serializable: ^4.1.0
  freezed_annotation: ^0.14.0

  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.0
  freezed: ^0.14.0
  retrofit_generator: ^2.0.1

flutter:

  uses-material-design: true
